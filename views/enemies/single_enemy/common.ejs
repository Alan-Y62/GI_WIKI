<div class="drops">
    <% type.drops.map(drop => { %> 
        <img src="../../../public/images/drops/<%= drop.toLowerCase() %>.png" alt="<%= drop %> ">
    <% }) %> 
</div>

<style>
    .drops{
        display: flex;
        flex-direction: row;
    }
</style>